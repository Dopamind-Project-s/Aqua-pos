# تدقيق حالة الـ TODO (اعتمادًا على الصورة + فحص الكود)

تاريخ التدقيق: 2026-03-16

## المنجز ✅
1. **إصلاح Hero بالعربي** — يوجد دعم اتجاه RTL ومفاتيح ترجمة مع تبديل لغة/ثيم في الواجهة.
2. **إصلاح Slider بالعربي** — يوجد تهيئة للكاروِسل مع دعم RTL في `main.js`.
3. **تحسين Responsive Navbar + Burger** — زر `navbar-toggler` موجود مع Collapse منظم.
5. **استبدال Team بـ Clients في الهاتف/المسار** — يوجد Route Redirect من `/team` إلى `/clients` + عنصر Clients في النافبار.
6. **إصلاح صور Clients** — مكوّن `client-card` يعرض الشعار مع placeholder ويستخدم التخزين.
7. **تحسين ألوان Contact** — صفحة contact مهيئة ضمن القالب الحالي وتمت إعادة تنسيقها سابقًا.
8. **تحسين ألوان أيقونات السوشال في Topbar** — أزرار السوشال لها كلاسات مخصصة داخل `header`.
10. **Auth + Login + Seeder + أدمن حقيقي** — كنترولر تسجيل دخول أدمن + Seeder لإنشاء Admin + Middleware routes.
14. **تنظيف الداشبورد من static data** — الداشبورد يعتمد على `$stats` الديناميكي.
15. **إزالة القديم + توحيد Buy Now/topstrip** — وجود Topstrip إداري موحد وروابط سريعة بدل الشكل القديم المتكرر.
16. **توحيد لوجو/اسم الأدمن مع إعدادات الموقع** — `layouts.admin` وDashboard يعتمدان على `siteSetting`.
17. **حذف الصفحات غير المستخدمة** — تم استبدال/إعادة توجيه `team` إلى `clients`.
18. **تحسين Sidebar + Admin layout + Scroll** — Sidebar بـ `overflow:auto` مع layout ثابت.
21. **Demo: auto-country من IP + علم الدولة** — موجود Fetch على `ipapi` مع حفظ الدولة والعلم تلقائيًا.

## المتبقي ⏳
4. **Navbar (ترتيب نهائي إضافي للـ UX)**
   - الوضع الحالي جيد لكن لا يوجد ملف/مهمة توضح إغلاق هذه النقطة رسميًا، وتحتاج مراجعة UX نهائية (خصوصًا سلوك القوائم والـ mega menu على الشاشات المختلفة).

9. **استكمال الترجمة 100% لكل الصفحات**
   - ما زالت هناك نصوص ثابتة إنجليزية بدون `data-i18n` (مثل بعض نصوص قسم المدونة في الصفحة الرئيسية).

11. **كامل CRUD + جدول صور + Slider + المنتجات متعددة الصور**
   - ✅ تم تنفيذ جزء المنتجات متعددة الصور: رفع متعدد، عرض، تعديل ترتيب/Alt، وحذف صور المنتج من شاشة التعديل.

12. **إدارة السكشن الرئيسية (Hero/Slider) من الأدمن**
   - إعدادات الموقع الحالية تغطي الشعار وبيانات عامة، لكن لا توجد شاشة إدارة مخصصة لمحتوى Hero/Slider للصفحة الرئيسية.

13. **Dark/Light بالهوم + مراجعة وربط Our Blogs**
   - Dark/Light موجود، لكن قسم `Our Blog` في `home` ما زال يحتوي روابط `#` ثابتة وغير مربوطة بمحتوى فعلي.

19. **Dashboard: إضافة تقارير أوسع**
   - الداشبورد الحالي يعرض بطاقات وإحصاء بسيط فقط، ولا توجد تقارير موسعة (Charts/Breakdowns/Trends).

20. **Requests: تعديل الحالات + فلترة متقدمة**
   - ✅ تم تنفيذها: تمت إضافة فلاتر متقدمة (بحث + الحالة + نطاق التاريخ) مع إمكانية تعديل الحالة مباشرة من جدول الطلبات داخل لوحة الأدمن.

## توصية تنفيذ سريعة (الأولوية)
1. ✅ **(20) Requests**: تم التنفيذ.
2. ✅ **(11) Product Images**: تم تنفيذ رفع متعدد + ترتيب/Alt + حذف.
3. **(12) Hero/Slider CMS**: CRUD لإدارة سلايدر الرئيسية من الأدمن.
4. **(9) Translation pass**: إغلاق كل النصوص الثابتة في الصفحات العامة.
5. **(13) Home Blog linking**: ربط كروت المدونة بمسارات `/blog` و`/blog/{slug}`.
